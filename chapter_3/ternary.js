//ternary operator

//condition ? true side : false side

var n = 11

var result = n % 2 == 0 ? 'Even' : 'Odd'
console.log(result)

// we can apply this way ternary operator
//console.log(n % 2 == 0 ? 'even' : 'odd')